# GitHub Achievements Manager Configuration
# Copy this file to .env and fill in your values

# ===========================================
# REQUIRED SETTINGS
# ===========================================

# Your GitHub Personal Access Token
# Generate at: https://github.com/settings/tokens
# Required scopes: repo (full control of private repositories)
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Your GitHub username (auto-populated during setup)
GITHUB_USERNAME=your-username

# Target repository for achievement creation (format: owner/repo)
# This is where PRs, issues, and commits will be created
# Example: your-username/GitHub-Achievements-Manager
TARGET_REPO=your-username/your-repo

# ===========================================
# OPTIONAL SETTINGS
# ===========================================

# Coauthor for Pair Extraordinaire achievement
# These appear in commit messages as "Co-authored-by: Name <email>"
COAUTHOR_NAME=n0
COAUTHOR_EMAIL=luke@u.software

# Branch prefix for created branches (default: achievement)
BRANCH_PREFIX=achievement

# Delay between operations in milliseconds (default: 1000)
# Increase if you're hitting rate limits
DELAY_MS=1000

# ===========================================
# ADVANCED SETTINGS
# ===========================================

# Enable verbose logging (default: false)
VERBOSE=false

# Enable test/dry-run mode (default: false)
# When true, shows what would happen without actually executing
TEST_MODE=false

# Batch size for parallel operations (default: 5)
BATCH_SIZE=5

# Log level: debug, info, warn, error (default: info)
LOG_LEVEL=info

# ===========================================
# GALAXY BRAIN (OPTIONAL)
# ===========================================

# Galaxy Brain achievement requires a second GitHub account
# The helper account creates questions, your main account answers them
# This token should be from a DIFFERENT GitHub account than GITHUB_TOKEN
# Generate at: https://github.com/settings/tokens (logged into helper account)
# Required scope: repo, write:discussion
# HELPER_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
